apiVersion: apps/v1
kind: Deployment
metadata:
  name: animal-website
spec:
  replicas: 2
  selector:
    matchLabels:
      app: animal-website
  template:
    metadata:
      labels:
        app: animal-website
    spec:
      containers:
      - name: nginx
        image: nginx:alpine
        ports:
        - containerPort: 80
        volumeMounts:
        - name: website-content
          mountPath: /usr/share/nginx/html
      volumes:
      - name: website-content
        configMap:
          name: animal-website-content