apiVersion: apps/v1
kind: Deployment
metadata:
  name: farm-website
spec:
  replicas: 2
  selector:
    matchLabels:
      app: farm-website
  template:
    metadata:
      labels:
        app: farm-website
    spec:
      containers:
      - name: nginx
        image: nginx:alpine
        ports:
        - containerPort: 80
        volumeMounts:
        - name: website-content
          mountPath: /usr/share/nginx/html
      volumes:
      - name: website-content
        configMap:
          name: farm-website-content